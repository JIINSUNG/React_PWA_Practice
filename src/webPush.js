// 서버 딴에서 vapidKeys를 생성 후, 클라이언트에 공개키 전달

// const webpush = require("web-push");

// // // VAPID keys should be generated only once.
// const vapidKeys = webpush.generateVAPIDKeys();

// export default vapidKeys.publicKey;
